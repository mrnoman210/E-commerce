// const index = require("../index");
// function findById(id) {
//   return new Promise((resolve, reject) => {
//     const product = index.find((p) => p.id === id);
//     resolve(product);
//   });
// }
// module.export = { findById };
